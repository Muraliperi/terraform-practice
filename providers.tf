
# Configure the AWS Provider
provider "aws" {
  region = "us-east-1"
  access_key = "xxyyzz"   # Not recommended to update access key and secret key here
  secret_key = "aabbcc" # Not recommended to update access key and secret key here
}
